export default function Page() {
  return (
    <main>
      <h1>Watchman</h1>
      <p>M0 — spine. Webhook at <code>/api/github/webhook</code>, jobs at <code>/api/inngest</code>.</p>
    </main>
  );
}
